/** Enum of check locations an item can be randomized to. */
public enum Check
{
    // Checks are individually numbered so they can be rearranged without causing
    // all the checks in-editor to be changed.
    FreeSpace = 0,
    Bushes = 1,
    Rocks = 2,
    Island = 3,
    GrassyKnoll = 4,
}
