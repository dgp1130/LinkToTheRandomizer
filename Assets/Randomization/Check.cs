namespace DevelWithoutACause.Randomizer
{
    /** Enum of check locations an item can be randomized to. */
    public enum Check
    {
        Bushes,
        Left,
        Middle,
        Rocks,
        GrassyKnoll,
    }
}
